#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
source .venv/bin/activate 2>/dev/null || true
exec uvicorn api.server:app --host 0.0.0.0 --port 8088 --reload
